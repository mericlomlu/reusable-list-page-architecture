# Absolute production URL used to build canonical links, Open Graph/Twitter
# metadata, the sitemap, and robots.txt (see src/lib/site-url.ts). Set this
# once the custom domain is live; until then the site falls back to the
# Vercel production URL, then to a local development URL.
NEXT_PUBLIC_SITE_URL=https://your-domain.example

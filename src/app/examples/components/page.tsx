import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function ComponentsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Examples / Components"
      title="Components"
      description="The searchable, filterable component library example is implemented in a later slice."
    />
  );
}
